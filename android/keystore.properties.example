# Copy to keystore.properties (same folder) and fill in. Do not commit keystore.properties.
# Values come from Expo: eas credentials -p android → Download keystore (passwords + key alias).

storeFile=keystores/@manojchow72__nestiti.jks
storePassword=
keyAlias=
keyPassword=
